[project]
name = "python-project-django"
version = "0.1.0"
description = ""
authors = [
    {name = "Дмитрий Глоба",email = "dima.globa2015@yandex.ru"}
]
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[dependency-groups]
dev = [
    "mypy (>=1.20.2,<2.0.0)",
    "ruff (>=0.15.11,<0.16.0)"
]
